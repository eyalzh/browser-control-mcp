{
  "name": "browser-control-mcp",
  "version": "1.5.0",
  "private": true,
  "bin": "mcp-server/dist/bin.js",
  "scripts": {
    "postinstall": "npm install --prefix mcp-server && npm install --prefix firefox-extension && npm run build --prefix mcp-server",
    "build": "nx run-many --target=build --all --parallel"
  },
  "devDependencies": {
    "nx": "22.3.3"
  }
}
